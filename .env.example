# Cosmic CLI Environment Configuration
# Copy this file to .env and modify as needed

# API Environment: "production" (default) or "staging"
# The SDK uses this to determine which Cosmic API to call
# COSMIC_API_ENV=production

# Dashboard API URL (optional, for local development)
# COSMIC_DAPI_URL=http://localhost:3000/v3

# Debug mode: Set to "1" to enable verbose logging
# COSMIC_DEBUG=1
